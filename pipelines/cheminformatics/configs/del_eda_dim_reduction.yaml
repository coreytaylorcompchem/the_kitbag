# Objective: run a standard package of analyses for DNA-encoded library data.
# Output: descriptive analyses useful for exploratory data analysis and 
# dimensionality reduction (PCA/t-SNE, UMAP) useful for ML model building.

workflow_name: streamed_feature_runner

input_file: data/ddr1.parquet

workflow:
  - compute_fingerprints_task
  - incremental_pca_task
  # - dimensionality_reduction_analyses_task

incremental_pca:
  enabled: true
  n_components: 50
  batch_size: 50000

output:
  directory: outputs/streamed_ddr1       # Where chunks + PCA results go
  filename: reduced_ddr1.parquet         # Combined PCA-reduced dataset

# workflow_serial:
#   - dimensionality_reduction_analyses
#   # - medoid_analysis
#   # - report_generation

# dimensionality_reduction_analyses:
#   input_file: outputs/streamed_ddr1/reduced_ddr1.parquet
#   pca:
#     n_components: 2
#   tsne:
#     n_components: 2
#     perplexity: 3
#     max_iter: 300
#   umap:
#     n_components: 2
#     metric: jaccard
#     n_neighbors: 30
#     min_dist: 0.1
#     random_state: 42
#   output_dir: outputs/dimensionality_reduction

# medoid_analysis:
#   input_file: outputs/streamed_ddr1/reduced_ddr1.parquet
#   scaffold: Murcko
#   output_dir: outputs/medoid_analysis

# report_generation:
#   input_file: outputs/streamed_ddr1/reduced_ddr1.parquet
#   clustering: 
#     type: DBSCAN
#     eps: 0.5
#     min_samples: 10
#   output_dir: outputs/descriptive_report

cleanup: true  # deletes intermediate steps after success
